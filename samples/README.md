# Local PCAP Samples

Place local demo captures in `samples/pcaps/`.

Packet captures can contain sensitive network data and may be large, so Netra
intentionally excludes `*.pcap`, `*.pcapng`, and `*.cap` files from Git. Use
captures only from systems and networks you are authorized to analyze.
