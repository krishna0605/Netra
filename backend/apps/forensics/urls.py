from django.urls import path

from apps.forensics import views


urlpatterns = [
    path("health", views.health),
    path("setup/status", views.setup_status),
    path("setup/admin", views.setup_admin),
    path("auth/login", views.auth_login),
    path("auth/refresh", views.auth_refresh),
    path("auth/logout", views.auth_logout),
    path("auth/me", views.auth_me),
    path("users", views.users),
    path("users/<str:user_id>", views.user_detail),
    path("cases", views.cases),
    path("cases/<str:case_id>", views.case_detail),
    path("cases/<str:case_id>/workspace", views.case_workspace),
    path("workspaces/<uuid:route_ref>", views.case_workspace_by_route),
    path("cases/<str:case_id>/summary", views.case_light_summary),
    path("cases/<str:case_id>/light-summary", views.case_light_summary),
    path("cases/<str:case_id>/charts", views.case_charts),
    path("cases/<str:case_id>/tabs/<str:tab_name>", views.case_tab),
    path("cases/<str:case_id>/reports", views.case_reports),
    path("cases/<str:case_id>/flags", views.case_flags),
    path("cases/<str:case_id>/flags/<str:flag>", views.case_flag_detail),
    path("cases/<str:case_id>/links", views.case_links),
    path("cases/<str:case_id>/links/<str:link_id>", views.case_link_detail),
    path("cases/<str:case_id>/anomaly-explanation", views.case_anomaly_explanation),
    path("cases/<str:case_id>/notes", views.case_notes),
    path("cases/<str:case_id>/history", views.case_history),
    path("cases/<str:case_id>/custody-ledger", views.custody_ledger),
    path("cases/<str:case_id>/custody-ledger/verify", views.custody_verify),
    path("cases/<str:case_id>/custody-ledger/export", views.custody_export),
    path("cases/<str:case_id>/linked-evidence", views.case_linked_evidence),
    path("cases/<str:case_id>/members", views.case_members),
    path("cases/<str:case_id>/members/<str:member_id>", views.case_member_detail),
    path("cases/<str:case_id>/linked-packets", views.link_stub),
    path("cases/<str:case_id>/linked-sessions", views.link_stub),
    path("cases/<str:case_id>/linked-payloads", views.link_stub),
    path("evidence/normalize-preview", views.evidence_normalize_preview),
    path("evidence/upload-sessions", views.evidence_upload_sessions),
    path("evidence/upload-sessions/<uuid:upload_session_id>", views.evidence_upload_session_detail),
    path("evidence/upload-sessions/<uuid:upload_session_id>/finalize", views.evidence_upload_session_finalize),
    path("evidence/upload", views.evidence_upload),
    path("evidence/<str:evidence_id>/manifest", views.evidence_manifest),
    path("evidence/<str:evidence_id>/verify-integrity", views.evidence_verify_integrity),
    path("evidence/<str:evidence_id>/download", views.evidence_download),
    path("capture/interfaces", views.capture_interfaces),
    path("capture/live/start", views.capture_live_start),
    path("capture/live/stop", views.capture_live_stop),
    path("capture/live/status/<str:job_id>", views.capture_live_status),
    path("capture/replay/start", views.capture_replay_start),
    path("capture/replay/stop", views.capture_replay_stop),
    path("capture/replay/<str:job_id>/stop", views.capture_replay_stop),
    path("capture/replay/status/<str:job_id>", views.capture_replay_status),
    path("capture/replay/<str:job_id>/status", views.capture_replay_status),
    path("capture/live/<str:job_id>/stop", views.capture_live_stop),
    path("capture/live/<str:job_id>/status", views.capture_live_status),
    path("capture/log-import", views.capture_log_import),
    path("sensors", views.sensors),
    path("sensors/register", views.sensor_register),
    path("sensors/<str:sensor_id>", views.sensor_detail),
    path("sensors/<str:sensor_id>/enable", views.sensor_enable),
    path("sensors/<str:sensor_id>/disable", views.sensor_disable),
    path("sensors/<str:sensor_id>/history", views.sensor_history),
    path("sensors/<str:sensor_id>/captures", views.sensor_captures),
    path("sensors/<str:sensor_id>/heartbeat", views.sensor_heartbeat),
    path("sensors/<str:sensor_id>/commands/next", views.sensor_next_command),
    path("sensors/<str:sensor_id>/chunks", views.sensor_chunk_upload),
    path("sensors/<str:sensor_id>/captures/<str:job_id>/complete", views.sensor_capture_complete),
    path("sensors/<str:sensor_id>/captures/<str:job_id>/fail", views.sensor_capture_fail),
    path("sensor-groups", views.sensor_groups),
    path("sensor-groups/<str:group_id>", views.sensor_group_detail),
    path("capture-schedules", views.capture_schedules),
    path("capture-schedules/<str:schedule_id>", views.capture_schedule_detail),
    path("capture-schedules/<str:schedule_id>/run-now", views.capture_schedule_run_now),
    path("capture-schedules/<str:schedule_id>/history", views.capture_schedule_history),
    path("events", views.operational_events),
    path("events/stream", views.operational_event_stream),
    path("logs/import/zeek", views.zeek_log_import),
    path("jobs/<str:job_id>/status", views.job_status),
    path("jobs/<str:job_id>/events", views.job_events),
    path("jobs/<str:job_id>/cancel", views.job_cancel),
    path("system/workers", views.system_workers),
    path("system/health/deep", views.system_health_deep),
    path("system/metrics", views.system_metrics),
    path("system/storage", views.system_storage),
    path("system/indexes", views.system_indexes),
    path("system/kafka", views.system_kafka),
    path("system/realtime", views.system_realtime),
    path("system/kafka/lag", views.system_kafka_lag),
    path("system/capacity", views.system_capacity),
    path("system/throughput", views.system_throughput),
    path("system/indexes/retention", views.system_index_retention),
    path("system/database", views.system_database),
    path("system/sensors", views.sensors),
    path("system/dead-letter", views.dead_letter),
    path("system/dead-letter/<str:event_id>/retry", views.dead_letter_retry),
    path("system/dead-letter/<str:event_id>/ignore", views.dead_letter_ignore),
    path("jobs/<str:job_id>/reprocess", views.job_reprocess),
    path("search", views.search),
    path("dashboard/summary", views.dashboard_summary),
    path("dashboard/traffic-timeline", views.traffic_timeline),
    path("dashboard/protocol-distribution", views.protocol_distribution),
    path("alerts", views.alerts),
    path("alerts/<str:alert_id>/status", views.alert_status),
    path("packets", views.packets),
    path("packets/<str:packet_id>", views.packet_detail),
    path("sessions", views.sessions),
    path("sessions/<str:session_id>", views.session_detail),
    path("sessions/<str:session_id>/timeline", views.session_timeline),
    path("decoder/summary", views.decoder_summary),
    path("decoder/<str:protocol>", views.decoder_protocol),
    path("payloads", views.payloads),
    path("payloads/<str:finding_id>", views.payload_detail),
    path("detection/rules", views.detection_rules),
    path("detection/matches", views.detection_matches),
    path("detection/matches/<str:match_id>/status", views.detection_match_status),
    path("anomalies", views.anomalies),
    path("anomalies/baseline-comparison", views.anomaly_baseline),
    path("anomalies/risk-timeline", views.anomaly_risk_timeline),
    path("graph", views.graph),
    path("graph/nodes/<str:node_id>", views.graph_node),
    path("graph/attack-path", views.graph_attack_path),
    path("reports/<str:case_id>/preview", views.report_preview),
    path("reports", views.reports),
    path("reports/<str:case_id>/generate-pdf", views.report_generate),
    path("reports/<str:case_id>/generate", views.report_generate),
    path("reports/<str:report_id>/download", views.report_download),
    path("exports", views.exports),
    path("exports/<str:export_id>", views.export_detail),
    path("exports/<str:export_id>/download", views.export_download),
    path("integrations", views.integrations),
    path("integrations/siem/export", views.siem_export),
    path("integrations/<str:integration_id>", views.integration_detail),
    path("integrations/<str:integration_id>/sync", views.integration_sync),
    path("integrations/<str:integration_id>/test", views.integration_test),
    path("integrations/<str:integration_id>/send-alerts", views.integration_send_alerts),
    path("integrations/<str:integration_id>/deliveries", views.integration_deliveries),
    path("integrations/<str:integration_id>/deliveries/<str:delivery_id>/retry", views.integration_delivery_retry),
    path("integrations/case-link", views.integration_case_link),
    path("integrations/case-link/<str:case_id>", views.integration_case_link_detail),
    path("compliance/checklist", views.compliance_checklist),
    path("compliance/roles", views.compliance_roles),
    path("compliance/security-posture", views.security_posture),
    path("audit/access-logs", views.access_logs),
    path("retention/policy", views.retention_policy),
    path("retention/preview", views.retention_preview_view),
    path("retention/execute", views.retention_execute),
    path("retention/runs", views.retention_runs),
    path("cases/<str:case_id>/legal-hold", views.case_legal_hold),
    path("cases/<str:case_id>/legal-review/checklist", views.case_legal_review_checklist),
    path("system/incident-readiness", views.system_incident_readiness),
    path("system/deployment-readiness", views.system_deployment_readiness),
    path("system/status-matrix", views.system_status_matrix),
    path("ml/model-status", views.ml_model_status),
    path("audit/export", views.audit_export),
]
